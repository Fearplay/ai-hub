# Desktop UI - Qt for Python (Qt 6.x)
PySide6>=6.7.0

# Icon set - QtAwesome bundles Font Awesome / Material Design Icons /
# Phosphor / Remix / Codicons inside its wheel and renders each glyph
# to a ``QIcon`` / ``QPixmap`` via the Qt font database. We standardise
# on Material Design Icons 6 (``mdi6.*``) - see ``src/qt/icons.py``.
# Used everywhere: sidebar entries, section headers, buttons, pills.
qtawesome>=1.4.0

# Native OS trust store for HTTPS (Windows CryptoAPI / macOS Security /
# Linux OpenSSL). Activated once at the top of main.py via
# truststore.inject_into_ssl(); fixes corporate-proxy / antivirus
# CERTIFICATE_VERIFY_FAILED errors that the bundled certifi store can't
# resolve. Per upstream guidance, only injected from the application
# entry point - never inside library / section code.
truststore~=0.10

# Mozilla CA bundle. Required by libcurl (curl_cffi -> yfinance) which
# does NOT inherit Python's SSL configuration patched by truststore. We
# point libcurl at certifi's PEM via the CURL_CA_BUNDLE env var in
# main.py so live market data works on stock Windows Python builds.
certifi>=2024.2.2

# AI providers (called only via src.services.ai_provider; sections never import these directly)
openai>=1.50.0
anthropic>=0.39.0

# OS-native API key storage (Windows Credential Manager / macOS Keychain / Linux Secret Service)
keyring>=25.0.0

# Web fetching + HTML cleanup for the job scraper
httpx>=0.27.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
# Headless-browser fallback for sites that 403 or render via JS. We
# launch the user's installed Edge / Chrome / Firefox via Playwright's
# ``channel=`` flag so no Chromium download is required.
playwright>=1.49.0

# Resume + LinkedIn export parsing
pypdf>=4.0.0
python-docx>=1.1.0

# Document export pipeline (Markdown -> HTML / DOCX / PDF)
markdown2>=2.5.0
reportlab>=4.0.0

# OS-level clipboard helper (cross-platform, no UI dependency)
pyperclip>=1.9.0

# Live market tickers + FX for AI Finance (free, no API key, hits only
# public Yahoo Finance endpoints; no user identification). Opt-out via
# Settings -> "Live market data".
yfinance>=0.2.40
